
/**
 * Description contare elementi di un array
 * 
 *
 * @param {*} array
 * @returns {number}
 */
function contaElementi(array) {
    let contatore = 0;
    for (let i = 0; i < array.length; i++) {
        contatore++;
    }
    return contatore;
   
}
