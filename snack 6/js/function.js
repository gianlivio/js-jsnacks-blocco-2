/**
 * Description rimuove l'ultimo elemento dell'array e restituisce un nuovo array privo 
 * 
 *
 * @param {*} array
 * @returns {*}
 */
function rimuoviUltimoElem(array) {
    const nuovoArray = array.slice(0, array.length -1);
    return = nuovoArray;
}S